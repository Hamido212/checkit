{
  "name": "checkit",
  "version": "0.2.0",
  "private": true,
  "description": "Ein kleines Echtzeit-Abfahrtsdisplay für Bremen.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "node --check server.js && node --check public/app.js && node --check public/site.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": "22.x"
  }
}
